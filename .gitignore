.*
!.gitignore
!.github/
!.env.example
!.agents
build
node_modules
src/pkjs/dev-config.js
# active-fixture.generated.js is committed (default: module.exports = null) so the
# repo can be imported/built directly by CloudPebble. Local fixture builds
# regenerate it; don't commit that version.
!opencode.jsonc
!AGENTS.md
